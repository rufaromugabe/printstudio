"use client";
import { useEffect, useState } from "react";
import { useParams } from "next/navigation";
type Shared = { name:string; version:number; document:{product:string;color:string;method:string;elements:{front:unknown[];back:unknown[]}} };
export default function SharedDesign(){const {token}=useParams<{token:string}>();const [data,setData]=useState<Shared|null>(null);const [error,setError]=useState("");useEffect(()=>{fetch(`${process.env.NEXT_PUBLIC_API_URL??"http://localhost:8080"}/v1/shared/${token}`).then(async r=>{if(!r.ok)throw new Error();return r.json()}).then(setData).catch(()=>setError("This share link is invalid or has expired."))},[token]);return <main style={{maxWidth:720,margin:"70px auto",padding:24,fontFamily:"Arial"}}><div className="brand"><span className="brand-mark">P</span><strong>PrintStudio</strong></div>{error?<h1>{error}</h1>:!data?<p>Loading shared design…</p>:<><p className="eyebrow">READ-ONLY DESIGN · VERSION {data.version}</p><h1>{data.name}</h1><div style={{background:"#f3f3ef",borderRadius:20,padding:40,marginTop:30}}><h2>{data.document.product}</h2><p>{data.document.method} · {data.document.elements.front.length} front elements · {data.document.elements.back.length} back elements</p><p>This immutable shared snapshot can be reviewed without workspace access.</p></div></>}</main>}
